# ChaosForge Changelog

| Date | Version | Change |
|---|---|---|
| 2026-07-25 | 1.24.0 | **Frozen-gauge meta-signal back-ported from RPE Bundle 1 (cross-repo audit find).** `DlqDepthGauge.refresh()` caught broker failures with only a log WARN — the depth gauges froze at their last value (0 at boot = false-healthy) and `DlqTriageBacklog` went blind with no visible signal, the exact failure class RPE fixed with `rpe.dlt.depth.poll_failures` and never got mirrored here. Now: (1) `chaosforge.dlq.depth.poll_failures` Counter (tag `topic`, registered at construction so `increase()` has a 0 baseline from boot), incremented in both catch paths — per-topic refresh failure, and consumer-open failure (increments every topic: all gauges stale); (2) `DlqDepthPollFailing` alert (`>5` failed polls in 10m sustained 5m, per topic, ticket) in `alerts.yml`; (3) promtool unit tests 10–12 in `alerts_test.yml` (fires on sustained failure with verbatim annotations, inert on a 2-poll blip, inert flat-0); (4) `DlqDepthGaugePollFailureTest` — unreachable-broker unit test proving boot registration at 0 and per-refresh increments, the path `DlqDepthGaugeIT`'s embedded broker cannot exercise. architecture specifications metric + SLO tables updated. |
| 2026-07-25 | 1.23.1 | **1.23.0's honest caveat closed — full test suite proven against the 2.4.0/boot4 classpath.** `./gradlew check` BUILD SUCCESSFUL (execution-service suite executed live, embedded-Kafka ITs included), then — because Gradle reported the other modules' test tasks up-to-date — a belt-and-suspenders `--rerun-tasks` forced execution of every remaining suite: chaosforge-common 9, chaosforge-avro-schemas 6 (C29 compatibility gate), edge-gateway 36, control-plane 80, execution-service 117 — **248 tests, 0 failures, 0 errors**. The 1.23.0 "gates any working claim" caveat is lifted; the `resilience4j-spring-boot4` swap and the `RuleSetLoader` collapse are verified working, not just compiling. |
| 2026-07-25 | 1.23.0 | **Three-repo over-engineering audit closure — Resilience4j Boot-4 alignment, one vestigial seam collapsed, repo git-tracked.** (1) **`resilience4j-spring-boot3` 2.3.0 → `resilience4j-spring-boot4` 2.4.0** (all three services, version catalog + accessor rename): the Boot-3-targeted integration module on a Boot 4.1 classpath risked silently unregistered CB/bulkhead autoconfig, and RPE already pins the boot4 module at 2.4.0 — cross-repo drift closed. Artifact verified on Maven Central (2.4.0 is `<latest>`); no source imports `springboot3` packages, so the swap is autoconfig-level only. (2) **`RuleSetLoader` collapsed into `CpRuleSetLoader`**: the interface promised a seam for a two-level-cache elaboration that was instead built *inside* `CpRuleSetLoader` (Caffeine + CB + bulkhead), leaving one impl and mock-only test usage — Mockito mocks the concrete class identically (`ExecutePhase` + 3 tests retyped). **Deliberately kept:** `CommandPayloadCodec`, `ResultPayloadCodec`, `TargetHealthProbe` — lambda test doubles (`CpPostgresIT.stubCodec`, `SteadyStateCheckTest.responses`) prove those seams are exercised; the audit's single-impl flag is overturned for them, not deferred. (3) Repo newly git-tracked (baseline + per-refactor commits; `.gitignore` verified to hold back key material and `build/`). **Honest caveat:** verified to `compileJava`+`compileTestJava` on all five modules plus `ExecutePhaseControlsTest` green — a full `./gradlew check` against the 2.4.0 bump has NOT yet run and gates any "working" claim. (4) `chaosforge-mtls-design.md` moved from repo root to `docs/` (same audit, placement nit); ADR-0531's two path references repaired in place — mechanical reference fix, same class as the 1.16.0 placeholder-ADR comment corrections. The 1.9.0 changelog mention stays as written (historical record of where the file was added). (5) `renovate.json` added — versions already have a single source of truth here (`libs.versions.toml`; no intra-repo drift possible, no gate needed); Renovate's value is prompt upgrade PRs, which shrinks the cross-repo divergence window with RPE (the resilience4j boot3/boot4 drift class this entry fixes). Majors gated behind dependency-dashboard approval per the catalog's VERIFY-BEFORE-BUILD discipline. |
| 2026-07-19 | 1.22.0 | **Standing DLQ-depth signal — arch-audit F5 closure (ADR-0542).** The DLQ had no way to answer "how many records await human triage right now": `chaosforge.dlq.routed` is a monotonic routing counter, and `.DLQ` consumer-group lag reaches 0 with poison present (the retry consumer acks hard poison it never republishes, ADR-0529). Fixed with a **human-triage watermark**, not another counter. (1) CP Flyway **V11** `dlq_triage_watermark` — one row per `(topic, partition)`, exclusive high-water, monotonic `GREATEST` upsert, `reviewed_by`/`reviewed_at` audit, no `tenant_id`. (2) **`DlqDepthGauge`** emits `chaosforge.dlq.untriaged_depth{topic,partition} = max(0, endOffset − max(reviewedOffset, beginningOffset))` via a read-only, group-less `KafkaConsumer` (the `DlqRecordReader` discipline — structurally cannot commit; `allow.auto.create.topics=false`); grain deliberately `(topic, partition)`, **not** `dlq_reason` (offset arithmetic can't split a per-record header without an O(depth) scan — ADR-0542 Alt 1); no `tenant_id`. (3) **`DlqTriageWatermarkController`** `PUT /v1/dlq/{topic}/reviewed` — OPERATOR-gated (inherits `/v1/dlq/**` → `hasRole("OPERATOR")`, ADR-0536), audited to the verified JWT subject, writes only the CP row (never a Kafka offset/ack/replay). (4) **`DlqTriageBacklog`** alert (`> 0 for 1h`, ticket) + promtool cases. **Statefulness carve-out:** the write is kept off `DlqTriageService`/`DlqRecordReader`, so the AI advisory path stays literally read-only — ADR-0518/ai-rules.md re-scoped to "the AI path never writes state; the operator watermark verb does". **Honest caveats (documented, not hidden):** the gauge counts everything past the watermark incl. retry-lane-drained records Kafka still retains, and it *trusts* the operator's watermark advance — a *review* backlog, not verified disposition; DLQ retention must exceed the triage SLA. Closure discipline: implementation + tests first, `./gradlew check` green (223 tests), docs aligned only after — architecture specifications SLIs/SLO now record the signal. Proven by `DlqTriageWatermarkIT` (6), `DlqDepthGaugeIT` (1), `DlqTriageWatermarkAuthIT` (4) on real Postgres + embedded Kafka, and `alerts_test.yml` promtool. Also fixed a pre-existing runbook drift (AI-triage endpoint `POST /v1/ai/...` → `GET /v1/dlq/{topic}/triage/{offset}`). |
| 2026-07-16 | 1.21.0 | **Config-sync/coverage audit closure — two doc-asserted-but-unimplemented capabilities implemented (trace_id/MDC, DLQ triage tier), one undocumented decision backfilled (ADR-0540), stale ADR wording amended.** Closure discipline: implementation first, docs aligned only after `./gradlew check` green — never the reverse. (1) **trace_id/MDC log correlation** (the rules files' `trace_id` structured-log mandate was aspirational — no MDC emission existed anywhere): Micrometer Tracing **Brave bridge** on all three services. Boot 4 module split *again* (third instance after Flyway/http-client): the actuator starter does NOT pull `spring-boot-micrometer-tracing-brave` — declared explicitly; the BOM-managed bridge artifact is `micrometer-tracing-bridge-brave` (1.x naming, via `micrometer-tracing-bom` 1.7.0). **MDC-only: no exporter on any classpath** — spans are never shipped (ADR-0505 posture unchanged; sampling 1.0 is safe because nothing exports). W3C traceparent propagation: gateway `controlPlaneWebClient` switched to the auto-configured observation-wired `WebClient.Builder` (mTLS connector logic unchanged; `MtlsControlPlaneWebClientHandshakeTest` updated for the new param) → CP server observation joins the gateway's trace; `KafkaTemplate.setObservationEnabled(true)` on both relays + exec listener containers observation-enabled (both factories) → exec pipeline logs join the **relay's** trace (deliberately NOT the originating HTTP request — the outbox row severs that context by design; stated in architecture specifications). Gateway additionally needs `spring.reactor.context-propagation: auto` (Boot default is `limited` — verified from the jar's config metadata; without it MDC dies at the first operator hop). Proven by `TraceIdMdcIT`: a real request through the observation filter yields a request-path log line whose MDC `traceId` is non-blank hex — and the test fails if the tracing autoconfig module is ever dropped (the ADR-0530-style module-split regression guard). Not yet log-asserted: gateway reactive + exec listener MDC (same bridge, config-wired). (2) **DLQ triage tier implemented** (ADR-0518 surface 2 / ai-rules Tier 2 — was designed-only; `dlq-triage-system.txt` was a dangling doc reference): `DlqTriageController` `GET /v1/dlq/{topic}/triage/{offset}?partition=N` (partition param added vs the ADR sketch — a record is addressed by topic+partition+offset; the DLQ has as many partitions as the command topic), `DlqTriageService` (`chaosforge.ai.triage.requests{dlq_reason}` — shape token, never tenant), `DlqRecordReader` (**read-only structurally**: group-less throwaway consumer cannot commit; **`.DLQ`-topics-only** so the endpoint can never become a generic Kafka payload reader; **redaction by construction** — `DlqEnvelope` has no payload/tenant/scenario fields; first-line, length-capped exception summary per ADR-0519), `OllamaTriageClient` (own bean on the shared `ollama-chat` CB+bulkhead, mirroring `OllamaAuthoringClient`, so reader 400/404s never poison the model circuit), `prompts/dlq-triage-system.txt` (advisory-only; taxonomy→`DISCARD\|INVESTIGATE\|REPLAY_WHEN_READY`). **OPERATOR-role-gated** in CP `SecurityConfig` — DLQ metadata is cross-tenant; authentication ≠ authorization (ADR-0536, same posture as the kill switch). `ResourceNotFoundException` gains a String ctor. Proven by `DlqTriageServiceTest` (404 never reaches the model) + `DlqTriageIT` (real embedded broker: a planted tenant UUID, Avro payload, and multi-line stack trace never surface in the envelope; operator→200, tenant→403, anonymous→401). (3) **Decision/doc closure from the 2026-07-15 audit:** **ADR-0540** backfilled (Lua sliding-window rate limit, true-global, fail-open on Redis failure — the implemented mechanism had no ADR and ADR-0500's Context still said "token-bucket"; ADR-0500 amended for that + the never-built SSE criterion); ADR-0525 Amendment 2 (wire serde = local Avro codec in-tx, not the Apicurio Kafka serdes its Implementation Notes named); ADR-0528 amended (`max.block.ms=2000`, both relays, arch-audit A-2); ADR-0505 amended (tracing = MDC-only, no collector); **architecture specifications partition-key line corrected to `scenario_id`** (it contradicted ADR-0526 — code was right, architecture specifications was stale); gateway-rules rate-limit section rewritten (sliding-window semantics + fail-open observability — "bucket refill" wording was stale); execution-service-rules gains the ADR-0523 phase vocabulary at first use. `.docs-lint-allow` audit-noise entries added (pom.xml negative example, `spring.ssl.bundle.pem` config key, generated `truststore.p12`) and the `dlq-triage-system.txt` entry removed same-day once the tier landed. Tests 179→186 (180 under `test` + 6 under `avroSchemaCompatibilityCheck`); `./gradlew check` clean. |
| 2026-07-14 | 1.20.0 | **Lab JWKS stub (the ADR-0524 "stand up an IdP" follow-up, local half) + JWT claim-validation hardening (iss/aud).** (1) **Static JWKS stub:** `docker/jwks/generate-jwks.sh` (RSA-2048 → git-ignored `keys/` + `public/jwks.json`, kid `chaosforge-lab-1`; pattern mirrors `generate-certs.sh`) + `docker/jwks/mint-jwt.sh` (pure openssl RS256 minting: `tenant_id`, `roles[]`, `--roles OPERATOR` for the kill switch; ttl ≥ 300s floor documented) + `jwks-stub` compose service (`nginx:1.27-alpine`, host :9000 = `JWK_SET_URI`'s default → zero application.yml changes; healthcheck fetches the JWKS itself; only `public/` is mounted so the signing key cannot be served). Verified live: served JWKS `n`/`e` == signing-key modulus/exponent byte-for-byte; minted-token signature `openssl dgst -verify` OK; and the same script pattern passed a full live-Spring auth matrix against RPE's stricter validators (401/200/401/401/403). The smoke-test `$TENANT_JWT` in architecture specifications is now mintable — previously no token source existed. (2) **Confused-deputy hole closed — decoders now validate iss+aud, matching what mtls-rules.md always claimed** ("signature, exp, iss, aud" — a doc-ahead-of-code drift: all three `SecurityConfig`s built `withJwkSetUri(...).build()`, which accepts ANY token signed against the JWKS regardless of intended issuer/relying party; weaker than RPE's posture, flagged in the 2026-07-14 three-repo audit). Each service's `jwtDecoder` bean (gateway reactive + CP + exec) now sets `jwtClaimsValidator(issuer, audience)`: `JwtValidators.createDefaultWithIssuer` (timestamps + iss exact-match) + `JwtClaimValidator` on `aud` (contains-check). Config `chaosforge.security.jwt.{issuer,audience}` (`JWT_ISSUER`/`JWT_AUDIENCE`), lab defaults exactly what `mint-jwt.sh` stamps (`http://localhost:9000` / `chaosforge`). Composed from Spring built-ins only — no new classes; duplicated per service because `chaosforge-common` is deliberately framework-free. Rejection surfaces as `JwtValidationException extends JwtException` → the existing 401 paths (CP filter catch + resource-server entry point). Proven by `SecurityConfigJwtValidatorTest` (×3 services, 6 cases each: iss/aud pass, multi-aud pass, wrong-iss/wrong-aud/missing-aud rejected, expired-still-rejected proves the default timestamp validator survives the customization). mtls-rules.md pins the invariant ("do not regress to the validator-less decoder"); `.env.example` documents the two new vars. Tests 161→179 (173 under `test` + 6 under `avroSchemaCompatibilityCheck`); `./gradlew check` clean. |
| 2026-07-13 | 1.19.0 | **Arch-audit reconciliation — 6 findings fixed (H1-H3, M1-M3), 5 verified already-resolved (M4, L1-L4), 2 ADRs added.** (1) **H1 — gateway rate limits were silently unenforced:** `ControlPlaneClient.fetchTenantPolicy` called the JWT-guarded `/v1/tenants/{id}` with no credential (always 401 → silent default-policy fallback for every tenant). Now calls a new peer-authenticated CP endpoint `GET /internal/tenants/{id}/policy`; `InternalMtlsSecurityConfig` (ADR-0532) extended from single-peer to **per-path** cert-CN scoping (`internal-gateway-cn` for the policy path, `internal-peer-cn` for rule-sets, `denyAll` elsewhere — fail-closed on any unmapped path). The fail-open fallback is no longer silent: WARN + `chaosforge.gateway.policy_load{outcome}` counter. ADR-0536. Proven by `ControlPlaneClientPolicyTest`, `InternalMtlsAuthorizationIT` (+3 cases). (2) **H2 — a run could publish two contradictory terminal events under DLQ retry:** `FinalizePhase.complete` inserted its result event before an unguarded `UPDATE scenario_run`, and the `IncompleteRunSweeper` keyed staleness on `started_at` (frozen at first claim) rather than retry activity — a run legitimately bouncing through the DLQ retry lane (fresh `message_id` each attempt, ADR-0529) could be swept INCOMPLETE mid-flight, then a later successful finalize re-flipped it and double-published. Fixed with two coupled changes: `RunLogDao.finalizeRun` is now status-guarded (`AND status='IN_PROGRESS'`) and returns rows-affected, gating the event on the winning transition; migration **V11** adds `scenario_run.last_attempt_at`, refreshed on every Phase-1 claim, and the sweep predicates on it instead of `started_at`. ADR-0535. Proven by `FinalizePhaseTest` (Docker-free) and `IncompleteRunSweepIT` (+2 cases: fresh-heartbeat protection, late-finalize-after-sweep no-op). (3) **H3 — several resilience mechanisms failed silently:** no alert on `chaosforge.partition.default_rows` (the one C28 failure that can't self-heal) or `outbox.pending_outside_claim_window`; the outbox DEAD/lag alerts were CP-only, missing the exec result-event relay; the command-accept P99 alert aggregated the intentionally-slow AI authoring path; the gateway rate-limit fail-open path was uninstrumented. Added `PartitionDefaultRowsPresent`, `OutboxStragglerBacklog`; broadened `OutboxDeadLetterPresent`/`OutboxRelayLagging` to `job=~"control-plane|execution-service"`; excluded `/v1/ai/.*` from `CommandAcceptLatencyP99High`; instrumented `RateLimitWebFilter` with `chaosforge.gateway.rate_limit{outcome}` + WARN, alertable via new `TenantPolicyFeedDown`/`RateLimitFailingOpen`. ADR-0537. Proven by `RateLimitWebFilterTest` (5 cases, added in a follow-up gap pass after the two new gateway alerts were found pointing at the wrong runbook — `dlq-and-outbox.md`, 0 mentions of rate-limit/policy; new `docs/runbooks/gateway-rate-limit-and-policy.md` written, `docs/runbooks/README.md` index reconciled for all 4 alerts this entry adds). (4) **M1 — the steady-state health probe could overshoot the aggregate deadline into a self-inflicted rebalance:** it reused the 30s-timeout `targetRestClient` and `SteadyStateCheck.breached()` only checked the deadline before the inter-probe sleep, not before the probe pass itself — at N≥2 distinct hosts one `allHealthy()` pass could cost enough to cross `max.poll.interval.ms`. New dedicated `probeRestClient` (1s connect / 2s read) + deadline check moved to the top of the loop. ADR-0538. Proven by `SteadyStateCheckTest`, `HttpTargetHealthProbeTest`. (5) **M2 — the kill switch was authenticated but not authorized:** `anyRequest().authenticated()` let any valid tenant JWT trip the global operator halt. Exec `SecurityConfig` now requires `hasRole("OPERATOR")` on `/internal/kill-switch`, with a `JwtAuthenticationConverter` mapping the `roles` claim to `ROLE_*` (mirroring gateway/CP convention). Folded into ADR-0536. Proven by `KillSwitchAuthorizationIT` (operator→200, tenant→403, none→401). (6) **M3 — `CommandDecoder.verifyMintClock` was documented but did not exist** (a doc-ahead-of-code drift: `partitioning-rules.md` and architecture specifications both cited it as the step-1 guard against a clock-skewed `message_id` poisoning the un-droppable `inbox_default` partition). Implemented at pipeline step 1, window derived from the same `chaosforge.partition.{inbox-retention-days,lookahead-days}` config the partition maintenance uses, so validation and partition coverage can never drift apart. ADR-0539. Proven by `CommandDecoderTest` (+4 cases). **Verified already-resolved this pass (no code change needed):** M4 (IPv6 ULA `fc00::/7` — `TargetUrlGuard.isUniqueLocalV6` already covers it, confirmed empirically against the JDK's `isSiteLocalAddress` gap), L1 (`dlq-partitions` default is 12, matching `command-partitions` — same-index DLQ routing no longer depends on `verifyPartition`), L2 (Lua script already declares `KEYS[2]` for the `:seq` counter), L3 (the two independent `600` rate-limit defaults already cross-reference each other), L4 (the stale inbox-DELETE changelog text was already replaced by the partition-DROP description). Tests 132→161 (155 under `test` + 6 under `avroSchemaCompatibilityCheck`); `./gradlew check` clean. |
| 2026-07-06 | 1.18.0 | **Results-topic wire contract + sweeper terminal events + DLQ-header PII sweep (EADIE Phase-1 P2 closure).** (1) **`ScenarioRunResult` Avro schema** (subject `chaosforge.scenario.results.v1-value`): FinalizePhase's placeholder payload (`outcome.getBytes()` — unreadable by any Avro consumer) replaced with schema-at-write Avro binary via the exec `AvroResultPayloadCodec` (local `SpecificDatumWriter`, no registry I/O in-tx — mirrors the CP command codec, ADR-0525). `outcome` is deliberately a **string, not an enum** — a new terminal state must never be a schema change (enum symbol additions are not FULL_TRANSITIVE-safe). The C29 gate now checks this subject too (own `avro-history/` + `shippedResultSchema_isFullTransitiveWithItsRegisteredHistory`); round-trip proven by `AvroResultPayloadCodecTest`. (2) **Swept runs now reach a terminal *event*, not just a terminal state:** `RunLogDao.markStaleRunsIncomplete` returns the swept identities via `RETURNING` (atomic with the status flip — a racing late Phase-3 finalize wins and is never overwritten) and `IncompleteRunSweeper` emits an `INCOMPLETE` result event per swept run through `FinalizePhase.insertResultEvent` (the single definition of the results contract) in the SAME tx. Pre-fix, a Phase-2→3 crash produced a run that no downstream results consumer could ever observe terminating. Proven by `IncompleteRunSweepIT` (decodes the emitted Avro payload; fresh/terminal runs emit nothing). (3) **PII rule enforced on the DLQ path:** `ExecutePhase`'s INFRA_TRANSIENT messages carried the raw target URL into DLQ headers (`kafka_dlt-exception-message`) and logs, and the steady-state breach log printed full health URLs — violations of target-validation-rules "shape token only". Now step-id / probe-count only. schema-rules.md canonical section updated. Tests 124→132 (126 under `test` + 6 under `avroSchemaCompatibilityCheck`); `./gradlew check` clean. |
| 2026-07-06 | 1.17.0 | **Outbox relay hardening (EADIE arch-audit G1/G2 reconciliation) — partition-pruned claim, pipelined publish, outage-proof DEAD gating; both relays (CP + Exec), deliberately mirrored.** (1) **Partition-pruned claim (the P1):** the relay claim's `WHERE message_id IN (subquery)` could not use the `(msg_ts, message_id)` PK on the range-partitioned outbox (no standalone `message_id` index survives V8/V9) → it seq-scanned every live partition on the 1s poll path, violating partitioning-rules §2 — the one relay statement the arch-audit-2.5 fix missed. Now a `MATERIALIZED` CTE carries the full PK through the self-join (per-row pruning) and orders by `next_attempt_at` so the inner scan rides `idx_*_outbox_due` in index order (the old `ORDER BY message_id` re-sorted the ENTIRE due set every tick — the 10x-backlog killer; relay ordering is best-effort, fence + inbox absorb reorder). Proven by `OutboxClaimLanesIT`/`ExecOutboxClaimLanesIT`: EXPLAIN of the exact production SQL at 150k-row volume asserts the populated partition is PK-probed and never seq-scanned, with the legacy shape kept as a **negative control** the same detector must flag (empty partitions legitimately show cost-0 Seq Scans — the assertion targets the populated one). (2) **Pipelined publish, ONE shared deadline, ONE batched finalize tx:** a partitioned broker now costs a tick ~1× send-timeout, not batch × 10s (proven: 8 never-acking sends complete in <3s at a 1s timeout), and finalize is 2 batched statements on 1 connection — never a per-record tx stampede against the Hikari-20 pool the CAS accept path shares. `max.block.ms=2000` on both producer factories (was default 60s: a full RecordAccumulator during an outage turned "async" dispatch into serial 60s stalls); a synchronous broker-global send() throw fast-fails the rest of the batch. (3) **DEAD gated on record-fatal faults:** pre-fix ANY failure at max-attempts (≈9.6 min of cumulative backoff) went terminal DEAD — a ≥10-minute broker outage would have mass-DEADed every in-flight row, forcing manual resurrection UPDATEs (exactly the data-repair the C31 game-day bar forbids). Now only RecordTooLarge/RecordBatchTooLarge/InvalidTopic/SerializationException can DEAD (`PublishFaultClassifier`, default = retain-and-retry — the deliberate opposite of the consumer DLQ's fail-closed default, argued in-code); broker-global failures back off at the cap forever under the relay-lag alerts. Regression: `brokerOutage_atMaxAttempts_staysPending_neverDead` (both services). (4) **Straggler lane + claim attribution (V10 ×2):** hot claim bounded by `claim-window-days` (2d, a plan-shape guardrail); a slow straggler lane (5-min cadence, oldest first) claims the complement so no PENDING row is architecturally unreachable before its partition drops; new gauge `outbox.pending_outside_claim_window` (alert > 0 sustained) + `claimed_by`/`claimed_at` columns + `outbox.lease_takeovers` counter make the at-least-once duplicate-publish window observable and attributable. Scheduler pools 3→4 (CP) / 4→5 (exec) for the new lane. partitioning-rules §2 rewritten to cover the claim; SLI table + architecture block updated. Tests 112→124 (119 under `test` + 5 under `avroSchemaCompatibilityCheck`); `./gradlew check` clean. |
| 2026-07-04 | 1.16.0 | **Doc-pair drift audit — two shipped-but-undocumented hardenings reconciled, plus a live regression fixed.** (1) **ADR-0532** (CP `/internal` tenant trust is peer-asserted, not JWT-bound) shipped its `@Order(1)` X.509 `InternalMtlsSecurityConfig` (restricts `/internal/**` to the exec client-cert CN, proven by `InternalMtlsAuthorizationIT` — exec cert→200, gateway cert→403, no cert→403) with no changelog entry. (2) **ADR-0534** (execution blast-radius containment) shipped a shared `TargetUrlGuard` (`chaosforge-common`) blocking SSRF-capable target URLs (metadata/loopback/RFC1918, plus an allowlist ceiling that overrides the block) at both authoring (`RuleSetService.create` → HTTP 422) and execution (`CpRuleSetLoader` → terminal `STEP_FAILED`), profile-gated off by default (`chaosforge.target.block-private-networks`) and on under `mtls`; proven by `TargetUrlGuardTest`. Both were fully implemented and already cited in the Rules table (`.architecture specifications/rules/target-validation-rules.md`) but never reached this changelog, the Acceptance Gates, or Known Limitations — surfaced by a doc-pair drift audit against README.md. **Live regression found and fixed while verifying:** `RuleSetService` (added for ADR-0534) imported the legacy `com.fasterxml.jackson.databind.*` (Jackson 2) instead of Boot 4's `tools.jackson.*` (Jackson 3 — ADR-0520) and called the Jackson-2-only `JsonNode.findValuesAsText`; no `com.fasterxml...ObjectMapper` bean exists in this app, so the whole Control Plane Spring context failed to start, taking every full-context IT down with it (`ControlPlaneContextIT`, `CpFlywaySchemaMigrationIT`, `TenantIdentityProvenanceIT`, `InternalMtlsAuthorizationIT` — 11 failures, all in control-plane). Fixed by switching the imports to `tools.jackson.*` and renaming to `JsonNode.findValuesAsString`; `./gradlew check` now passes clean. Tests 101→112 (107 under `test` + 5 under `avroSchemaCompatibilityCheck`). Also: ADR-0502's advisory-lock design was superseded by ADR-0522/ADR-0528 in three other documents but never got its own amendment banner (added, matching the ADR-0508 pattern); two code comments citing placeholder `ADR-0008`/`ADR-0007` (no such ADRs exist) corrected to the real `ADR-0504` (two-level cache) and `ADR-0523` (claim-phase inbox dedup); the Canonical File Map's ADR range corrected from "ADR-0500 through ADR-0531" to "ADR-0500 through ADR-0534" (README.md corrected to match). |
| 2026-06-30 | 1.15.0 | **Acceptance gate C20 closed — auto-abort on steady-state breach, wired to target health.** New `steadystate` subsystem in the Execution Service: `TargetHealthProbe`/`HttpTargetHealthProbe` (a real HTTP GET to the target's `/health`, reusing the bounded target `RestClient`; 2xx healthy, else/refused/timeout unhealthy), `SteadyStateGuard` (derives the probe URL from the scenario's first-step target `<scheme>://<authority>` + `health-path`; config-gated), and the stateful `SteadyStateCheck` (breaches after `max-consecutive-failures` consecutive unhealthy probes; resets on recovery so transient blips don't abort). `ExecutePhase` probes it before each fresh fault step (third stop-control beside the C19 kill switch + deadline) → breach finalizes the run `ABORTED`. Metric `chaosforge.steady_state.breach_total`. **Latent C19 bug fixed:** the V6 `scenario_run` CHECK rejected `ABORTED`, so kill-switch/steady-state finalize would have hit a constraint violation in prod (the C19 unit tests mocked the DAO so it was invisible) — exec migration **V9** adds `ABORTED`. Proven by `SteadyStateCheckTest` (5), `HttpTargetHealthProbeTest` (4, real local server), `ExecutePhaseControlsTest` (+2), and `SteadyStateAbortPipelineIT` (breach → ABORTED → finalize persists to real Postgres — the V9 regression guard). Runbook `docs/runbooks/steady-state-abort.md`. Tests 89→101. **All engineering acceptance gates now closed; only C31 (a survived staging game day) remains — an operational exercise, not code.** |
| 2026-06-30 | 1.14.0 | **Acceptance gate C19 closed — kill switch + in-executor deadline under PG-down / Kafka-partition.** New in-memory `KillSwitch` (`io.chaosforge.execution.control`) + operator endpoint `POST/DELETE/GET /internal/kill-switch` (mTLS channel + re-verified JWT), gauge `chaosforge.executor.kill_switch.engaged`. `ExecutePhase` checks it at each step boundary (before the deadline): engaged → halt fault injection, finalize the run `ABORTED` (terminal, NOT retried, NOT DLQ'd). The switch is deliberately dependency-free (one volatile read) so it works during exactly the outage it exists for. The in-executor aggregate deadline already existed; **added DB timeout hardening** to exec `application.yml` (`connectionTimeout` 5s, `socketTimeout` 30s, `statement_timeout` 30s — all ≪ the 5m poll interval) so a hung/partitioned DB op is a bounded failure, never a consumer stall → rebalance storm. Made the exec relay send-timeout configurable. Proven by `KillSwitchTest`, `ExecutePhaseControlsTest`, `ExecutorControlsUnderFaultIT` (real container: **PG paused** → DB read aborts at `socketTimeout`; **PG stopped** → kill switch still aborts with the DB down — via `dockerClient.pause/unpause/stop`), and an `ExecOutboxRelayIT` **Kafka-partition** case (never-acking send → bounded by send-timeout, row stays PENDING). Runbook `docs/runbooks/kill-switch.md`. Also fixed a C28-stale comment (exec scheduler now runs poll/maintain/sweep, not purge). Tests 77→89. |
| 2026-06-30 | 1.13.0 | **Acceptance gate C28 closed — inbox/outbox purge by partition-drop.** Exec `inbox`+`outbox` (migration V8) and CP `outbox` (V9) are now RANGE-partitioned by a `msg_ts` day-key; purge is an O(1) `DROP` of an aged partition via a new `PartitionMaintenance` bean (create-ahead on `ApplicationReadyEvent` + drop-old `@Scheduled`), replacing the `DELETE … WHERE status='SENT'`/`purgeSent` path (removed from both pollers + relay DAOs) so there are no dead tuples for autovacuum to chase under load. **Load-bearing design:** a dedup table cannot be partitioned by *processing* time (a redelivery gets a fresh `processed_at` → lands in another partition → composite PK never conflicts → dedup breaks); resolved by deriving the inbox `msg_ts` from the UUIDv7 `message_id` (`UuidUtil.getInstant`, deterministic) so a redelivery always maps to the same partition and `ON CONFLICT (msg_ts, message_id)` is exactly `message_id` dedup. The outbox has no dedup → `msg_ts DEFAULT now()` (no producer change). Proven by `PartitionMaintenanceIT` (exec — dedup-holds-across-redelivery + aged inbox/outbox day dropped O(1) while fresh rows survive) and `OutboxPartitionPurgeIT` (CP). The age-based drop's safety net is the C26/C27 `OutboxRelayLagging`/`OutboxDeadLetterPresent` alerts (runbook updated). Tests 71→77. |
| 2026-06-30 | 1.12.0 | **Acceptance gate C29 closed — FULL_TRANSITIVE schema gate + deploy ordering.** New Gradle task `avroSchemaCompatibilityCheck` (a `check` dependency; excluded from the default `test` task so it runs once) executes the real Avro `SchemaCompatibility` algorithm in `SchemaFullTransitiveCompatibilityTest` against the registered schema history in `chaosforge-avro-schemas/src/test/resources/avro-history/` — the registry's `FULL_TRANSITIVE` check, done locally and deterministically with no live Apicurio. Controls cover add-optional (pass), add-required/remove-required (fail), and the load-bearing **transitive-violation** case (a change that is FULL vs its immediate predecessor but rejected vs an older ancestor — proving the check is transitive, not pairwise-latest). The gate was verified end-to-end to **break the build** when a required field is added to the canonical schema, then go green on revert. Consumer-before-producer deploy ordering (+ rollback) documented in `docs/schema-compatibility.md`. JUnit 6.0.3 + AssertJ 3.27.7 added to the version catalog for the plain `java-library` avro module (no `spring-boot-starter-test`). Tests 66→71 (the +5 run under the gate task, not `test`). |
| 2026-06-30 | 1.11.0 | **Acceptance gates C10, C14/C16, C26/C27 closed (cheap-wins cluster).** (1) **C10** — HTTP-layer tenant-provenance proof (was deferred): `TenantIdentityProvenanceIT` drives the real Spring Security filter chain (mocked `JwtDecoder` via Boot 4 `@MockitoBean`; `MockMvc` built with `webAppContextSetup(wac).apply(springSecurity())` because Boot 4 moved `@AutoConfigureMockMvc` out of the starter into a separate webmvc-test module) and proves a tenant-B JWT spoofing `X-Tenant-Id: <tenant-A>` → 404 with no `ETag`/version disclosed, the inverse spoof equally inert, missing/invalid JWT → 401. (2) **C14/C16** — C16 (crash-after-claim reaper) was already proven by `IncompleteRunSweepIT`; added the missing C14 advance-past-poison assertion to `DlqRoutingE2EIT` (`consecutivePoisonRecords_bothReachDlq_partitionNeverStalls` — two consecutive un-decodable records both reach `.DLQ`, so the partition provably did not stall on the first). (3) **C26/C27** — `docker/prometheus/alerts.yml` (completion-rate ≥95%/10m, stuck-scenario ≤1%/15m, + hard-poison/outbox-DEAD/relay-lag/accept-P99 backstops) wired via `rule_files`, verified loaded **and** evaluating `ok` in the running Prometheus; per-alert runbooks in `docs/runbooks/`. Alertmanager routing is the production add-on (lab has none). Tests 60→66. |
| 2026-06-29 | 1.10.0 | **VT-pinning harness (was deferred) — Option-1 "accept pinning" now measured, not assumed.** The CP runs JDBC on the request virtual thread with no dedicated `jdbcExecutor`; on JDK 21 a blocking `synchronized` section pins, so the safety of this rests entirely on the driver+pool. `VirtualThreadJdbcPinningIT` (CP) drives 64 VTs into real PgJDBC 42.7 + Hikari 7 row-lock + pool-starvation contention while an in-process JFR `Recording` captures `jdk.VirtualThreadPinned` (the pinned-**and**-blocked, carrier-starving kind) and asserts **zero** — plus a negative-control test that deliberately pins (`synchronized` + `Thread.sleep` on a VT) to prove the recorder is sighted (a clean result would otherwise be vacuous). Empirical finding: PgJDBC 42.7 / Hikari 7 use `ReentrantLock` around blocking I/O, so the VT unmounts cleanly — Option 1 needs no executor. Corrected the fictional `jvm.threads.virtual.pinned` Micrometer gauge reference in `application-dev.yml` (pinning is a JFR signal, not a meter); noted Exec's consumer JDBC runs on the listener's platform threads so VT-pinning doesn't apply there. Tests 58→60. |
| 2026-06-12 | 1.0.0 | Initial |
| 2026-06-13 | 1.1.0 | Lean rewrite; AI claim + ADRs added |
| 2026-06-13 | 1.2.0 | Boot 4.1.x + Spring AI 2.0 |
| 2026-06-14 | 1.3.0 | Critical Constraints removed → `.architecture specifications/rules/`; ADR index removed → `docs/adrs/`; Rules section added |
| 2026-06-14 | 1.4.0 | Production hardening: (1) durable fencing max-seen in inbox_fence table; (2) outbox SENT gated on Kafka send callback; (3) inbox TTL cleanup job; (4) rate limit → true global Redis Lua sliding window; (5) DLQ x-dlq-reason taxonomy + INFRA_TRANSIENT/STEP_TIMEOUT retry consumer; (6) step execution aggregate timeout bounded below max.poll.interval.ms; (7) advisory lock replaced with CAS optimistic lock on replay_version — CP now horizontally scalable; (8) Gateway→CP ReactiveResilience4j CB; (9) Apicurio Schema Registry for chaosforge.scenario.commands.v1; (10) 409 returns Retry-After: 2; (11) mTLS for CP + Exec intra-service auth; (12) BeanOutputConverter<ScenarioDraft> @Valid gate on AI output |
| 2026-06-14 | 1.5.0 | Consistency pass: (1) Avro binary confirmed as wire format — ADR-0525; (2) CAS UPDATE targets `scenario_replay_state` — table ref fixed; (3) JWT re-verified at CP/Exec — ADR-0524; (4) JpaRepository ban + ArchUnit rule; (5) rate-limit fail-open stated as explicit posture; (6) cache worst-case staleness = L2 TTL; (7) inbox dedup >7d boundary documented |
| 2026-06-14 | 1.6.0 | C3: expected_version = ETag/If-Match from prior GET (read-then-CAS in same tx is invalid). C5: build tool = Gradle (Avro plugin; Maven removed). C25/C27: SLI table + SLO table added. C8: rate-limit fail-open now references Resilience4j CB wrap. Acceptance gates added. |
| 2026-06-29 | 1.9.0 | **Intra-service mTLS wired (was TODO) — ADR-0531.** The channel-auth half of ADR-0524 was paper-only: both client wiring points carried a `TODO` and CP/Exec accepted plain HTTP from siblings. Now: profile-gated `mtls` (`application-mtls.yml` per service) with `server.ssl.client-auth: need` on CP+Exec; Gateway `WebClient` + Exec CP `RestClient` attach the `internal-mtls` `SslBundle` via Boot 4 `HttpClientSettings` (builders in `spring-boot-http-client`, declared explicitly — web/webflux starters don't pull it, mirrors the ADR-0530 module split). Bundle attached iff `chaosforge.mtls.client-bundle` set, else fail-fast (no silent downgrade). Secrets env-only (passwords no default). Lab CA generator `docker/mtls/generate-certs.sh` (git-ignored). Proof: `MtlsControlPlaneClientHandshakeTest` (Exec/JDK) + `MtlsControlPlaneWebClientHandshakeTest` (Gateway/Netty) run a `needClientAuth(true)` server and assert mutual-TLS succeeds + no-cert is rejected. `chaosforge-mtls-design.md` added (was a dangling file-map ref). |
| 2026-06-29 | 1.8.0 | **Flyway startup migration fixed (P0) — ADR-0530.** Boot 4 moved `FlywayAutoConfiguration` into the separate `spring-boot-flyway` module; bare `flyway-core` no longer auto-migrates, so neither service was provisioning its schema at startup (masked by ITs that ran Flyway by hand). Fix: `spring-boot-starter-flyway` in both services (+ `clean-disabled`/`validate-on-migrate`). Observability: `FlywaySchemaObservability` → `db.schema.version` / `.migrations.applied` / `.migrations.pending` gauges + startup log. Guards: `FlywaySchemaMigrationIT` (exec) + `CpFlywaySchemaMigrationIT` (CP). |
| 2026-06-28 | 1.7.0 | Hardening pass: (1) L2 cache fail-open now instrumented (`cache.l2.errors{cache,op,reason}`) — degradation no longer silent; (2) broad `catch(Exception)` narrowed at the outbox pollers + Avro decoder (Error now propagates); (3) **DLQ retry consumer implemented** — ADR-0529: republishes replayable reasons with a **fresh** message_id over step-level idempotency (original-id reuse would be inbox-deduped to a no-op), exponential backoff + full jitter, `RETRY_EXHAUSTED` terminal; new SLI `chaosforge.dlq.retry{outcome,dlq_reason}`. `.architecture specifications/rules/dlq-rules.md` reconciled. |
