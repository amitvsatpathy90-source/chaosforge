# Doc-lint allowlist
#
# Files referenced from this repo's markdown that legitimately live OUTSIDE
# this repo (the sibling P1/RPE portfolio project), and are therefore expected
# to never resolve to a path inside this repo. Seeded by the docs sanitization audit.

# ADR-0525:55,164 — P1/RPE's Kafka topology standards doc, tagged `[P1]/kafka-topology-map.md`.
kafka-topology-map.md

# ADR-0507:23 — P1/RPE's own ADR log, tagged `[P1]/arch-decisions-log.md`.
arch-decisions-log.md

# ADR-0516:185-188 — RPE ADR files, tagged `[RPE]/docs/adrs/ADR-NN.md`.
ADR-01.md
ADR-06.md
ADR-12.md
ADR-13.md
ADR-15.md

# --- Intentional references to files that do not (yet) exist in this repo ---
# Seeded by the 2026-07-15 config-sync audit. Each entry carries its reason;
# REMOVE the entry when the underlying condition changes.

# chaosforge-mtls-design.md + mtls docs — generated output of
# docker/mtls/generate-certs.sh; git-ignored, exists only after the script runs.
truststore.p12

# ai-rules.md — deliberate NEGATIVE example ("Gradle only, not pom.xml");
# never a real reference.
pom.xml

# ADR-0531 — Spring config-key namespace (spring.ssl.bundle.pem), not a file;
# false positive of the .pem extension heuristic.
spring.ssl.bundle.pem
